export enum CandidateJobStatus {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
  IN_INTERVIEW = 'in_interview',
  HIRED = 'hired',
  SHORTLISTED = 'short_listed',
  LOW_QUALIFY = 'low_qualify',
  OVER_QUALIFY = 'over_qualify',
  IN_REVIEW = 'in_review'
}
